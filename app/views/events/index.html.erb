  <div class="container">
  <h2 class="text-center">Your search results:</h2>
  </div>

<div class="pt-0 pb-0 pl-5 pr-5 mt-4 events_index row justify-content-center">
  <!-- <div class="row card-row"> -->
  <% @events.each do |event| %>
    <div class="col-4">
      <div class="card ml-5 mb-5" >
        <% if event.photo.url %>
          <%= image_tag event.photo.url, class: "img-card-top" %>
        <% else %>
          <img src="https://images.unsplash.com/photo-1528712306091-ed0763094c98?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=500&q=60" class="img-card-top">
        <% end %>
      <div class="price">$ <%= event.price %></div>
      <div class="card-body">
        <h5 class="card-title"><%= event.type_of_event %> - <%= event.type_of_meal %></h5>
        <p class="card-text"><%= event.title %></p>
        <%= link_to "More details", event, class: "btn btn-flat btn-details" %>
      </div>
      </div>
    </div>
  <% end %>
</div>
<%= paginate @events %>
